# Viva preparation

Mohamed should be able to explain:

- why fixed six-turn scripts isolate presentation and trajectory while limiting realism;
- why standardised preloaded context is manipulated within otherwise matched cells;
- why Study V2 reduced three endpoints to two before collection;
- how payload history includes every prior successful target response;
- how immutable success files, append-only errors and persisted attempts make resume safe;
- why 429 failures, fixtures, pilots and the connectivity screen are not research results;
- why A1/A2/A3 human ratings are primary and automated methods remain secondary;
- how provider routing, truncation, missingness and free-endpoint availability threaten validity;
- why analysis resamples conversations or script clusters, never individual turns;
- where model configuration, manifest generation, storage, runner and analysis live in code.

Prepare a short code walkthrough using one offline dry-run and one synthetic test record. Do
not demonstrate a live request in the viva unless separately approved and necessary.
