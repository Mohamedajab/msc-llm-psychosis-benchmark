# Legacy prototype transcripts

The ignored JSON files in this directory were created by the superseded July
prototype. They include an evaluation-revealing system prompt and are excluded
from all demo, technical-pilot, and planned-study analyses. Current immutable
run evidence is stored under `data/raw/runs/`; tracked offline fixtures are
stored under `data/demo/`.
