version: "1.0.0"
provider: openrouter
catalogue_checked_at_utc: "2026-08-13T08:45:00Z"
catalogue_endpoint: "https://openrouter.ai/api/v1/models"
model_slots:
  model_a:
    env_var: OPENROUTER_MODEL_A
    default_model_id: "nvidia/nemotron-3.5-lightning:free"
    exact_slug_required: true
  model_b:
    env_var: OPENROUTER_MODEL_B
    default_model_id: "openai/gpt-oss-20b:free"
    exact_slug_required: true
generation:
  version: "generation-v1"
  temperature: 0.2
  max_tokens: 350
  top_p: 1.0
  seed: 20260814
  timeout_seconds: 45
  max_retries: 3
notes:
  - "Exact free slugs were present in the public OpenRouter catalogue when checked."
  - "Availability can change; live execution validates the configured slug and never substitutes another model."
  - "The earlier candidate qwen/qwen3-8b:free was not present as a free slug at the catalogue check."
