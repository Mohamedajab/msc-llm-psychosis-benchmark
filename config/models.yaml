version: "2.0.0"
provider: openrouter
catalogue_checked_at_utc: "2026-08-29T10:41:49.746448Z"
catalogue_endpoint: "https://openrouter.ai/api/v1/models"
model_slots:
  model_minimax:
    default_model_id: "minimax/minimax-m3:free"
    exact_slug_required: true
  model_nemotron:
    default_model_id: "nvidia/nemotron-3-super-120b-a12b:free"
    exact_slug_required: true
generation:
  version: "generation-v2"
  temperature: 0.2
  max_tokens: 512
  top_p: 1.0
  seed: 20260814
  timeout_seconds: 45
  max_retries: 3
provider_routing:
  version: "provider-routing-v1"
  allow_fallbacks: false
  require_parameters: true
  pinned_providers: {}
  minimum_context_tokens: 16384
repetition_seeds:
  1: 20260814
  2: 20260815
notes:
  - "Study V2 uses two exact zero-priced candidates selected before main-study collection: MiniMax M3 and NVIDIA Nemotron Super."
  - "Nemotron passed a one-attempt connectivity screen on 2026-08-29; finish_reason=length at 350 tokens motivated generation-v2's prespecified 512-token limit."
  - "The connectivity screen is technical evidence only and does not qualify Nemotron for the main study; Pilot V4 remains required."
  - "Provider pinning is not asserted because a stable free provider-routing slug has not been qualified; every response records the resolved provider."
  - "Live execution performs current exact-slug, zero-price, text-modality, seed and context checks and never substitutes a model."
