# Retired prototype metadata

These three JSON files are retained only as historical inputs from the July
prototype. They are not loaded by the current experiment and are never sent to
a target model. Presentation is encoded only in the frozen six-turn scripts in
`config/scenarios/`.
